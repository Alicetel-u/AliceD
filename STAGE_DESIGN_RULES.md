# ステージ作成・変更に関する絶対ルール

このドキュメントは、本プロジェクトにおけるステージ作成および変更に関する不可侵のルールを定めたものです。
AIアシスタントは、以下のルールを厳守しなければなりません。

## 1. ルール変更のプロセス
*   **許可制**: AIが既存のステージ作成ルールを変更したい場合、または例外的な実装を行いたい場合は、必ず**事前に日本語でユーザーに説明し、明示的な許可を得なければならない**。
*   **独断禁止**: ユーザーの許可なく、勝手に設計思想や配置システムの大枠を変更してはならない。

## 2. ボスステージ（Boss Area）の完全保護
*   **不可侵領域**: ステージの「ボス戦エリア（Boss Arena）」には、いかなる変更も加えてはならない。
    *   地形（床、穴、壁など）の変更禁止。
    *   アイテム、敵、ギミックの追加配置禁止。
*   **実装上の注意**: `StagePlacements.js` やその他の配置ロジックを実装する際は、必ず `px < preBossLength` などのガード条件を設け、物理的にボスエリアに影響が及ばないようにコードを記述すること。

## 3. ステージ構成（ボス前）
*   **目的**: ユーザーを楽しませる、リズムとフローのあるステージ作りを目指す。
*   **手段**: `src/data/StagePlacements.js` 等の定義ファイルを使用し、制御可能な状態で配置を行う。

## 4. オブジェクト配置の具体ルール
*   **加速リング (Speed Ring / 'O')**:
    *   **地面配置禁止**: 加速リングは必ず高い空中に配置すること（**y >= 5** 推奨）。
    *   低空ではなく、明確な空中アクションとして機能させるため。

## 5. 開始直後のセーフゾーン
*   **開始30ブロック以内の禁止事項**:
    *   敵（E, F）の配置禁止。
    *   穴（H）、壁（W）などの進行を妨げるギミックの配置禁止。
    *   プレイヤーが操作を確認し、助走をつけるための安全地帯を確保すること。

## 6. 雲ギミックの配置ルール ('C')
*   **高さ制限**: 雲足場は地面付近（y < 4）に配置しないこと。
    *   高所アクション用として使用し、視認性と役割を明確にするため。

## 7. 進行不能（詰み）の回避
*   **壁 (W)**: 壁を配置する場合、必ずそれを飛び越えるための**ジャンプ台 (J)** を直前に設置すること。
    *   単なる足場（B）では高さ不足で詰むリスクがあるため。
*   **穴 (H)**: 4ブロック以上の連続する穴には、中継点となる足場（B）や加速ギミックを用意すること。
*   **検証**: いかなる配置においても、標準的なプレイヤースキルで突破可能であることを論理的に確認すること。

## 8. スピードアップアイテム ('S') の制限
*   **個数制限**: 1ステージにつき最大2個までとする。
*   **配置間隔**: 複数配置する場合、プレイヤーが速度制御を取り戻せるよう、十分な間隔（目安として50ブロック以上）を空けること。

## 9. スタート地点後方の配置禁止
*   **配置禁止エリア**: スタート地点より後方（画面左側、目安として x < 5）には、アイテムやオブジェクトを一切配置しないこと。
    *   プレイヤーは右方向に進行するため、背後のアイテムは視認しにくく、取得も困難であるため。

## 10. ジャンプ台 ('J') の接地ルール
*   **接地保証**: ジャンプ台は必ず地面、またはブロック（B）の上に完全に接地していること。浮遊配置は禁止。
*   **穴との距離**: 落とし穴（H）の縁（前後1ブロック）にはジャンプ台を配置しない。
    *   物理的に不安定に見える配置や、踏み切り時の不安感を避けるため。

## 11. アイテムとギミックの重複禁止
*   **重なり判定**: ギミック（ジャンプ台、敵、壁、穴など）とアイテム（ニンジン、スピード、箱）を同一座標、または視認性を損なう近距離に配置しないこと。
*   **優先順位**: ギミックの視認性と機能を優先し、重なるアイテムは削除または移動すること。

## 12. ヘッドスペース（頭上空間）の確保
*   **最低高さ**: 空中ブロックや敵を配置する場合、地面から最低でも **4ブロック分** の高さを空けること。
*   **傾斜への配慮**: 階段や坂道の上に幅のあるオブジェクト（雲やブロック列）を置く場合、最も高い地面を基準に高さを確保すること。
    *   左端の高さ基準で配置すると、右側の登り坂で詰まる可能性があるため。

## 13. 足場とギミックの重複禁止
*   **埋まり防止**: 敵、ジャンプ台、加速リングなどのギミックを配置する場合、その座標および隣接マスに足場ブロック（B, #, D）が重ならないようにすること。
    *   ギミックがブロックに埋まって見えなくなったり、動作不良を起こすのを防ぐため。
    *   配置箇所は事前に空間（空気）であることを保証する。

## 14. 接地型敵の穴上配置禁止
*   **物理法則の遵守**: 飛行能力を持たない敵（タイプE, Wなど）を、足場のない場所（落とし穴の上など）に配置してはならない。
*   **対策**: 固定配置で座標指定する場合、必ずその下に地面またはブロックがあることを確認するか、システム側で自動削除すること。

## 15. 落とし穴内部への配置禁止
*   **デスゾーンの明確化**: 落とし穴（地面が存在しない列）の深部（画面下端付近、y > rows - 4 等）には、いかなるオブジェクト（敵、アイテム、ブロック）も配置してはならない。
*   **理由**: プレイヤーが落下死を受け入れるべき場所で、無意味なアイテム取得や衝突判定が発生するのを防ぐため。

## 16. 動く足場の有意義化
*   **報酬の配置**: 動く足場（タイプMなど）を配置する場合、その移動先や経路上に必ず「取得可能なアイテム（ニンジンなど）」を配置し、プレイヤーがその足場に乗る動機付けを行うこと。
*   **無意味な配置の禁止**: ただ動いているだけで、乗っても何もない、または進行上乗る必要が全くない配置は避ける。

## 17. 落とし穴上のブロック配置禁止
*   **穴の純粋性の維持**: 落とし穴（地面が存在しない列）の上空に、静的なブロック（タイプBなど）を配置してはならない。足場が必要な場合は、動く床（M）や雲（C）などのギミックを使用すること。
*   **自動削除**: システムは穴の上に配置された静的ブロックを検出し、自動的に削除する。

---
**このファイルの内容は、ユーザーの命令と同等の優先度を持つ。**
